
// capitalize
const colors = ['red', 'green', 'blue']
const numbers = ['one', 'two', 'three']

capitalizedColors = colors.map(x => x.charAt(0).toUpperCase() + x.substring(1))
console.log(capitalizedColors);
console.log(numbers);
